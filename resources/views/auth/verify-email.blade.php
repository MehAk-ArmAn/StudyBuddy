@extends('layouts.app')
@section('content')<section class="auth-page sb-verified-page"><div class="auth-panel auth-form compact sb-verified-card"><p class="eyebrow">Email verification</p><h1>Check your inbox to unlock StudyBuddy.</h1><p class="soft-copy">For safety, dashboards, quests, points, and adult/teacher controls stay locked until your email is verified.</p>@if(session('status'))<p class="status-note">{{ session('status') }}</p>@endif<form method="POST" action="{{ route('verification.send') }}">@csrf<button class="btn" type="submit">Send verification email again</button></form><form method="POST" action="{{ route('logout') }}" class="sb-inline-form">@csrf<button class="btn btn-ghost" type="submit">Logout</button></form></div></section>@endsection
