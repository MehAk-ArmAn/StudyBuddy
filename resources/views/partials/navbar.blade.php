<nav class="nav"><a class="brand" href="{{ route('home') }}">@if(!empty($settings['logo_path']))<img src="{{ asset($settings['logo_path']) }}" alt="{{ $settings['brand_name'] ?? 'StudyBuddy' }}">@endif<span>{{ $settings['brand_name'] ?? 'StudyBuddy' }}</span></a><button class="nav-toggle" data-nav-toggle>Menu</button><div class="nav-links" data-nav-links>@foreach($navigationItems as $item)<a href="{{ $item->url }}" @if($item->opens_new_tab) target="_blank" rel="noopener" @endif>{{ $item->label }}</a>@endforeach@if(!empty($settings['global_cta_label']))<a class="nav-cta" href="{{ $settings['global_cta_url'] ?? '#top' }}">{{ $settings['global_cta_label'] }}</a>@endif</div></nav>
