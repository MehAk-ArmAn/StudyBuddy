@php
    $imageUrl = $imageUrl ?? function (?string $path): ?string { if (blank($path)) return null; return preg_match('/^https?:\/\//i', $path) ? $path : asset($path); };
    $appStoreUrl = $settings['app_store_url'] ?? '#';
    $playStoreUrl = $settings['google_play_url'] ?? '#';
    $apps = $appSection?->items ?? collect();
    $categories = $apps->map(fn ($item) => $item->badge_text ?: 'Explore')->unique()->values();
@endphp
<section class="launcher-shell" data-app-launcher><div class="launcher-console"><div><p class="eyebrow">Game launcher</p><h2>Choose your learning world.</h2><p>Pick a mini app, preview the mission, filter by skill, or download the StudyBuddy app when mobile builds go live.</p></div><div class="store-buttons" aria-label="App download links"><a class="store-btn" href="{{ $playStoreUrl }}" @if($playStoreUrl !== '#') target="_blank" rel="noopener" @endif><span>Get it on</span><strong>Google Play</strong></a><a class="store-btn" href="{{ $appStoreUrl }}" @if($appStoreUrl !== '#') target="_blank" rel="noopener" @endif><span>Download on the</span><strong>App Store</strong></a></div></div><div class="launcher-personalizer" data-launcher-recommender><div><p class="eyebrow">Personalized picks</p><h3>Tell StudyBuddy your vibe.</h3><p data-recommendation-copy>Choose a role and time target to preview your recommended first quest.</p></div><div class="choice-cloud"><button type="button" data-recommend-role="student">Student</button><button type="button" data-recommend-role="parent">Parent</button><button type="button" data-recommend-role="teacher">Teacher</button><button type="button" data-recommend-role="independent">Independent Learner</button></div><div class="time-cloud"><button type="button" data-recommend-time="5">5 min</button><button type="button" data-recommend-time="10">10 min</button><button type="button" data-recommend-time="20">20 min</button></div></div><div class="launcher-tabs"><button class="active" type="button" data-app-filter="all">All worlds</button>@foreach ($categories as $category)<button type="button" data-app-filter="{{ Str::slug($category) }}">{{ $category }}</button>@endforeach</div><div class="launcher-grid">@foreach ($apps as $item) @php $category=Str::slug($item->badge_text ?: 'Explore'); $image=$imageUrl($item->image_path); @endphp <article class="launcher-card" data-app-card data-category="{{ $category }}" data-title="{{ e($item->title) }}" data-subtitle="{{ e($item->subtitle) }}" data-body="{{ e($item->body) }}" data-image="{{ e($image) }}"><div class="launcher-card-art">@if ($image)<img src="{{ $image }}" alt="{{ $item->title }}">@endif @if ($item->badge_text)<span>{{ $item->badge_text }}</span>@endif</div><div class="launcher-card-copy"><h3>{{ $item->title }}</h3>@if ($item->subtitle)<p>{{ $item->subtitle }}</p>@endif<div class="launcher-card-actions"><button type="button" data-open-app>Preview Mission</button>@if ($item->button_label)<a href="{{ $item->button_url ?: '#top' }}">{{ $item->button_label }}</a>@endif</div></div></article>@endforeach</div></section><div class="mission-modal" data-mission-modal hidden><div class="mission-dialog" role="dialog" aria-modal="true" aria-labelledby="mission-title"><button class="mission-close" type="button" data-close-mission aria-label="Close mission">×</button><img data-mission-image alt="" hidden><p class="eyebrow">Mission preview</p><h2 id="mission-title" data-mission-title></h2><p data-mission-subtitle></p><p data-mission-body></p><div class="mission-actions"><a class="btn btn-primary" href="/register">Save to My Quest</a><button class="btn btn-ghost" type="button" data-close-mission>Keep Browsing</button></div></div></div>
