@php $needsVerification=auth()->check()&&auth()->user()->needsAdultVerification();$adultStatus=auth()->check()?(auth()->user()->adult_verification_status??'not_required'):'not_required';@endphp
@if($needsVerification || !auth()->user()->hasVerifiedEmail())<article class="auth-panel sbv-dashboard-card"><p class="eyebrow">Trust status</p><h2>Account safety checks</h2><p>Email verification and adult/role verification protect parent, teacher, and independent learner controls.</p><div class="sbv-pills compact"><span>Email: {{ auth()->user()->hasVerifiedEmail() ? 'Verified' : 'Pending' }}</span><span>Adult: {{ str_replace('_',' ',$adultStatus) }}</span><span>Role: {{ str_replace('_',' ',auth()->user()->role_verification_status ?? 'not_required') }}</span></div><a class="btn btn-ghost" href="{{ route('studybuddy.verification.center') }}">Open Verification Center</a></article>@endif
